import numpy as np

# Ch 1: NDRel / hypergraph --> PreConcept / bipartite graph

# BiGraph --> vector space

def vector_space(xset:set, yset:set):
    basis = 
    for s in [xset, yset]:
        dicts.append({e:1 for e in s})
        

    # vector addition:
